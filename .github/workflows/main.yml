#---------------------------------------------------------------------
# GitHub Action Workflow to Deploy Flask App to AWS ElasticBeanstalk
#
# Version      Date        Info
# 1.0          2019        Initial Version
#
# Made by Denis Astahov ADV-IT Copyleft (c) 2019
#---------------------------------------------------------------------
name: MY-CI-CD-Pipeline-to-AWS-ElasticBeanstalk
env:
  EB_PACKAGE_S3_BUCKET_NAME : "hello-flask-app"
  EB_APPLICATION_NAME       : "hello-flask-app"
  EB_ENVIRONMENT_NAME       : "HelloFlaskApp-env"
  DEPLOY_PACKAGE_NAME       : "hello-flask-app-${{ github.sha }}.zip"
  AWS_REGION_NAME           : "ap-southeast-2"

on: 
  push:
    branches: 
      - master

jobs:
    my_ci_pipeline:
       steps:
         - name: Intro
           run: echo "Starting...."
           
       
        
